const initialState = {
    isAuth: false,
    name: '',
    id: ''
};

export default function admin(state = initialState, action = {}) {
    switch(action.type) {

        default: return state;
    }
};